# Plan: Merge all branches

## Objective
Bring all relevant branches in this repository to a single up-to-date branch with conflict-safe merges and clear verification.

## Current repository snapshot (read-only discovery)
- Local branches: `friday`, `main`
- Remote branches: `origin/friday`, `origin/main`, `origin/master`
- Current branch: `friday`
- Notable divergence: `main` is at `69a809f`; `friday` is ahead with additional commits; `origin/master` points to `406605b`

## Proposed execution plan
1. **Decide canonical target branch**
   - Choose where all branches should be merged into (recommended: `main`).
   - Decide whether `master` should be treated as legacy and merged as well.

2. **Synchronize refs safely**
   - Fetch latest remote updates.
   - Verify clean working tree before merge operations.

3. **Merge branch set into target**
   - Checkout target branch.
   - Merge `friday` into target.
   - Merge `origin/master` (or local `master` equivalent) into target if requested.
   - Resolve conflicts file-by-file preserving newest intended behavior.

4. **Verification**
   - Run project build/tests (or the repo’s standard validation commands).
   - Confirm merged history with graph/log checks.

5. **Publish and branch hygiene**
   - Push updated target branch to `origin`.
   - Optionally align/retire old branch (`master`) after confirmation.

## Conflict policy (default)
- Prefer target branch behavior unless incoming branch contains newer feature/fix.
- For docs-only conflicts, keep more complete and current content.
- Re-run validation after each resolved conflict batch.

## Deliverables after implementation
- One updated canonical branch containing selected branches.
- Short merge report listing merged branches, conflicts resolved, and validation outcomes.
